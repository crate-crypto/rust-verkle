// create a new struct called MemoryDb which encapsulates what we have now
// We will use it to also implement "Cache the first three layers"

// pub trait Database {
//     type NodeIndex;

//     fn children(&self, parent: NodeIndex) -> Vec<(usize, NodeIndex)>;

//     fn child(&self, parent: NodeIndex, child_node_index: usize) -> Option<ChildDataIndex>;
// }
